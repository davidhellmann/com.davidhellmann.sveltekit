query GetAboutEntry(
  $site: [String] = ["davidhellmann_en"]
  $slug: [String] = ["about"]
  $limit: Int = 1
) {
  entries(
    site: $site,
    section: ["pages"],
    slug: $slug,
    limit: $limit
  ) {
    ...page_about
  }
}
