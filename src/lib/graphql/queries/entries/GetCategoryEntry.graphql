query GetCategoryEntry(
  $site: [String] = ["davidhellmann_en"]
  $slug: [String]
  $limit: Int = 1
) {
  entries(
    site: $site,
    section: ["categories"],
    slug: $slug,
    limit: $limit
  ) {
    ...page_category
  }
}
