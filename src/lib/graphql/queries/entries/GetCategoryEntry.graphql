query GetCategoryEntry(
  $site: [String] = ["davidhellmann_en"]
  $section: [String]! = ["categories"]
  $slug: [String]
  $limit: Int = 1
) {
  entries(
    site: $site,
    section: $section,
    slug: $slug,
    limit: $limit
  ) {
    ...page_category
  }
}
