query GetHomeEntry(
  $site: [String] = ["davidhellmann_en"]
  $section: [String]! = ["home"]
  $limit: Int = 1
) {
  entries(
    site: $site,
    section: $section,
    limit: $limit
  ) {
    ...page_home
  }
}
