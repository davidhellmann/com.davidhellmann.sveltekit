query GetWorkListPage(
  $site: [String] = ["davidhellmann_en"]
  $limit: Int = 1
) {
  entries(
    site: $site,
    section: ["pages"],
    type: ["page_workList"],
    limit: $limit
  ) {
    ...page_workList
  }
}
