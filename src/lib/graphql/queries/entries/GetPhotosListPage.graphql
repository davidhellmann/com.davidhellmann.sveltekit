query GetPhotosListPage(
  $site: [String] = ["davidhellmann_en"]
  $section: [String]! = ["pages"]
  $limit: Int = 1
) {
  entries(
    site: $site,
    section: $section,
    type: ["page_photosList"],
    limit: $limit
  ) {
    ...page_photosList
  }
}
