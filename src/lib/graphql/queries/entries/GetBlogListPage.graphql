query GetBlogListPage(
  $site: [String] = ["davidhellmann_en"]
  $limit: Int = 1
) {
  entries(
    site: $site,
    section: ["pages"],
    type: ["page_blogList"],
    limit: $limit
  ) {
    ...page_blogList
  }
}
