query GetTopicEntry(
  $site: [String] = ["davidhellmann_en"]
  $section: [String]! = ["topics"]
  $slug: [String]
  $limit: Int = 1
) {
  entries(
    site: $site,
    section: $section,
    slug: $slug,
    limit: $limit
  ) {
    ...page_topic
  }
}
