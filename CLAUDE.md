# CLAUDE.md

This repository uses `AGENTS.md` as the canonical agent guidance file.

Claude Code should follow the instructions in `AGENTS.md` for workflow, development commands, architecture notes, and content structure.

## Agent skills

### Issue tracker

Issues are tracked as local markdown files under `.scratch/<feature-slug>/`; external PRs are not a triage surface. See `docs/agents/issue-tracker.md`.

### Triage labels

The repo uses the default five-role triage vocabulary. See `docs/agents/triage-labels.md`.

### Domain docs

This repo uses a single-context domain-doc layout. See `docs/agents/domain.md`.
